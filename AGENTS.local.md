# Local-only context

## External brand restriction

The app uses mastery-based learning language:
- Mastery gating: previous set must reach mastery before next unlocks.
- Accuracy threshold: 95%+ to pass, 100% + speed target for mastery.
- Repetition-based skill building with per-level speed targets.
- Progressive difficulty across levels (7A → O for math, 7A → L for languages).

Do not mention or reference the external worksheet-learning brand whose name starts with `K` and rhymes with `new-mon` anywhere in code, comments, UI text, commit messages, docs, tests, content, generated output, or filenames. Use terms like "mastery-based learning", "small-step practice", or "progressive skill building" instead.
